# Public Examples

These fictional, sanitized examples show the public request and output shapes. They do not contain customer material, model credentials, local paths, request IDs, or internal diagnostics.

- `simple-mode-input.json`: a compact Simple mode request.
- `professional-mode-input.json`: a Professional mode request with explicit facts and safety boundaries.
- `customer-version-example.md`: an editable audience-facing script excerpt.
- `production-version-example.md`: a production-facing slide specification excerpt.

Examples are illustrative. They are not model-output benchmarks and do not imply a particular local model configuration.
